//just a workspace 

1. turn float aine table into fix15 for lfo -- no scale, keep it -1 < x < 1

mod knob scales 0-499
paramA knob scales 0-999

rate knob should be mHz denominated, scales  from 100-10000 (later try slower values & push into audio range) 

all values are thousandths so << 12 should make them fix15 with the decimal in the right place 

